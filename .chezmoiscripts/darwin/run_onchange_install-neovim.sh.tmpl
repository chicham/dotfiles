#!/bin/bash
# {{ template "scripts/shell-header.tmpl" }}
# vim: set ft=bash:

# Install neovim on macOS using Homebrew
if ! command -v nvim >/dev/null 2>&1; then
  echo "Installing neovim..."
  brew install neovim
else
  # Check if we need to upgrade
  if brew outdated | grep -q neovim; then
    echo "Upgrading neovim..."
    brew upgrade neovim
  else
    echo "neovim is already installed and up to date."
  fi
fi

# Create config directory if it doesn't exist
mkdir -p "${HOME}/.config/nvim"
