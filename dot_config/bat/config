# This is `bat`s configuration file. Each line either contains a comment or
# a command-line option that you want to pass to `bat` by default. You can
# run `bat --help` to get a list of all possible configuration options.

# Set theme
--theme="Catppuccin Mocha"

# Enable this to use italic text on the terminal
--italic-text=always

# Keep default paging behavior - this will be overridden by smart_bat function in VS Code
--style=numbers,changes,header

# Syntax mappings
--map-syntax ".ignore:Git Ignore"
--map-syntax ".gitconfig:Git Config"
--map-syntax "*.conf:Config"
--map-syntax "*.fish:Fish"
