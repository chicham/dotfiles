alias ls="exa"
alias cat="bat"
alias ll="ls -al --icons"
alias ln="ln -v"
alias mkdir="mkdir -p"
alias e="$EDITOR"
alias g="git"
# alias gs='git stash'
alias gl="glab"

# Pretty print the path
alias path='echo $PATH | tr -s ":" "\n"'
