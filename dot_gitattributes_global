# Global .gitattributes file
# This is a template managed by chezmoi

# --- Universal Settings ---

# Auto-detect text files and enforce LF line endings on commit.
# This is the most important setting for cross-platform compatibility.
* text=auto eol=lf
* merge=mergiraf

# --- Diff Settings ---

# Don't show diffs for generated, binary, or lock files.
*.min.js  -diff
*.min.css -diff
*.lock    -diff
*.svg     -diff
*.png     -diff
*.jpg     -diff
*.gif     -diff
*.zip     -diff
*.pdf     -diff

# --- Application-Specific Settings ---

# Jupyter notebooks: Use nbdime for clean diffs and merges.
# (Requires nbdime to be installed and configured)
*.ipynb diff=jupyternotebook
*.ipynb merge=jupyternotebook

# --- LFS Template ---

# The following LFS configuration is commented out as a template.
# To use Git LFS, enable it per-repository by adding rules to the
# project's local .gitattributes file.

# # LFS files
# *.psd     filter=lfs diff=lfs merge=lfs -text
# *.pdf     filter=lfs diff=lfs merge=lfs -text # Example if you want PDFs in LFS
# *.zip     filter=lfs diff=lfs merge=lfs -text
# *.mov     filter=lfs diff=lfs merge=lfs -text
# *.mp4     filter=lfs diff=lfs merge=lfs -text
